@using Vidly.Models
@model IEnumerable<Vidly.Models.Movie>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>
@Html.ActionLink("New Movie", "New", null, new { @class = "btn btn-primary rounded-2" })

<table id="movies" class="table table-hover table-bordered">
    <thead>
        <tr>
            <th>Movie</th>
            <th>Genre</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var item in Model)
            {
                <tr>
                    <td> @Html.ActionLink(item.Name, "Edit", new { id = @item.Id })</td>
                    <td>@item.Genre.Name</td>
                </tr>
            }*@
    </tbody>
</table>

@section Scripts
{
    <script src="~/Scripts/Movies/MoviesIndex.js"></script>
    <script src="~/Scripts/Movies/MoviesDataAccess.js"></script>
}



